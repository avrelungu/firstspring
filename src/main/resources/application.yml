server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/avrel
    username: avrel
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet